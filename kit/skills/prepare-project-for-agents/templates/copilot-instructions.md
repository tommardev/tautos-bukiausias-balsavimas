Follow the root `AGENTS.md` as the source of truth for this repository.

When working in a specific area, **Read** the matching helper listed under Pointers in `AGENTS.md` (stack, conventions, best-practices, ui-ux, theming, testing, security). Do not wait for a vendor glob.

Project helpers (`repo:`) win over common defaults when they are more specific. Golden defaults (`golden:`) apply only where this repo is silent.

Do not fork these rules into this file. Change `AGENTS.md` instead.
