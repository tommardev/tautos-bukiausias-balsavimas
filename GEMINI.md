# Antigravity Project Instructions

Follow root `AGENTS.md` as the source of truth for project layout, commands, conventions, boundaries, and on-demand helper docs (`docs/agents/*`).

Always-on house workflow rules are located in `.agents/rules/ai-coding-native-rules.md`.
